---
phase: 07-reports-polish
plan: 02
type: execute
wave: 15
depends_on: [07-01]
files_modified:
  - frontend/src/pages/project/ReportsPage.tsx
  - frontend/src/components/reports/VelocityChart.tsx
  - frontend/src/components/reports/BurndownChart.tsx
  - frontend/src/components/reports/KPICard.tsx
  - frontend/src/components/reports/KPIDashboard.tsx
  - frontend/src/hooks/useReports.ts
autonomous: true

must_haves:
  truths:
    - "Velocity chart: bar chart with planned vs completed story points"
    - "Burndown chart: line chart with ideal vs actual remaining points"
    - "KPI cards: total tasks, velocity, sprint progress, team size"
    - "Charts use Recharts with dark theme styling"
    - "Export CSV button downloads file"
---

<objective>
Build the frontend reports page: velocity chart, burndown chart, KPI dashboard, and CSV export button.

Purpose: Visual representation of team performance. Data-driven insights for retrospectives.
Output: Reports page with interactive charts and KPI cards.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reports hooks</name>
  <files>frontend/src/hooks/useReports.ts</files>
  <action>
- `useVelocity(projectId)`: fetch velocity data
- `useBurndown(projectId, sprintId)`: fetch burndown data for active sprint
- `useDashboardKPIs(projectId)`: fetch KPIs
- `useExportCSV(projectId)`: mutation that downloads blob as file
  </action>
</task>

<task type="auto">
  <name>Task 2: Charts</name>
  <files>frontend/src/components/reports/VelocityChart.tsx, frontend/src/components/reports/BurndownChart.tsx</files>
  <action>
1. `VelocityChart.tsx` (Recharts BarChart):
   - X axis: sprint names
   - Two bars per sprint: planned (blue, lighter) + completed (blue, solid)
   - Dashed line: average velocity
   - Dark theme: dark grid, light text, blue bars
   - Glass card wrapper
   - Tooltip on hover: sprint name, planned pts, completed pts
   - Animate on mount

2. `BurndownChart.tsx` (Recharts LineChart):
   - X axis: dates of sprint
   - Two lines: ideal (dashed gray) + actual (solid, color-coded)
   - Actual line: green if below ideal, red if above
   - Area fill below actual line (subtle gradient)
   - Today marker: vertical dashed line
   - Dark theme styling
   - Tooltip: date, ideal remaining, actual remaining
  </action>
</task>

<task type="auto">
  <name>Task 3: KPI dashboard</name>
  <files>frontend/src/components/reports/KPICard.tsx, frontend/src/components/reports/KPIDashboard.tsx</files>
  <action>
1. `KPICard.tsx`:
   - Glass card
   - Icon (Lucide) + label (Inter 500) + value (JetBrains Mono 32px bold)
   - Optional: trend arrow (up/down) with percentage
   - Count-up animation on mount (useMotionValue)
   - Color accent based on metric type

2. `KPIDashboard.tsx`:
   - Grid: 4 KPI cards in a row (desktop), 2x2 (tablet), stack (mobile)
   - Cards: Total Tareas, Completadas, Velocidad Promedio, Progreso Sprint
   - Below: VelocityChart + BurndownChart side by side (stacked on mobile)
   - CSV export button at top right
  </action>
</task>

<task type="auto">
  <name>Task 4: Reports page</name>
  <files>frontend/src/pages/project/ReportsPage.tsx</files>
  <action>
- Tab in project layout
- Content: KPIDashboard → VelocityChart → BurndownChart
- Empty states when no completed sprints ("Completa tu primer sprint para ver reportes")
- Export button: "Exportar Backlog (CSV)" — triggers file download
- Page entrance animation
  </action>
</task>

</tasks>

<success_criteria>
- Velocity chart renders with correct data from API
- Burndown shows ideal vs actual progression
- KPI cards show correct metrics with count-up animation
- CSV downloads correctly
- Charts use dark theme with blue accents
- Empty states when no data
</success_criteria>
