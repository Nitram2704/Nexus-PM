---
phase: 02-project-management
plan: 03
type: execute
wave: 5
depends_on: [02-01]
files_modified:
  - frontend/src/pages/project/ProjectSettingsPage.tsx
  - frontend/src/components/projects/MemberList.tsx
  - frontend/src/components/projects/InviteMemberModal.tsx
  - frontend/src/components/projects/RoleSelector.tsx
  - frontend/src/pages/project/ProjectLayout.tsx
autonomous: true

must_haves:
  truths:
    - "Project settings page shows editable name/description and member list"
    - "Owner/Admin can invite members by email"
    - "Owner/Admin can change member roles"
    - "Project layout has tab navigation: Backlog, Tablero, Reportes, Settings"
---

<objective>
Build member management: invite by email, role assignment, member list. Create project layout with tab navigation.

Purpose: Enables team collaboration. The project layout (tabs) is the container for Phases 3-7 content.
Output: Working member management + project layout with tab navigation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Project layout with tabs</name>
  <files>frontend/src/pages/project/ProjectLayout.tsx</files>
  <action>
`ProjectLayout.tsx`:
- Fetches project detail on mount (`useProject(id)`)
- Header bar: project key badge + project name + sprint status indicator
- Tab navigation: Backlog | Tablero | Reportes | IA Agent | Settings
- Active tab has animated underline (slide effect)
- Content area: renders child route via `<Outlet />`
- Sidebar updates to show project-specific nav items
- Provides project context to children via React Context or URL params
  </action>
</task>

<task type="auto">
  <name>Task 2: Member list and role management</name>
  <files>frontend/src/components/projects/MemberList.tsx, frontend/src/components/projects/RoleSelector.tsx</files>
  <action>
1. `MemberList.tsx`:
   - List of members with: avatar, name, email, role badge, joined date
   - If current user is Owner/Admin: role is editable via dropdown
   - Owner badge is special (gold/amber accent)
   - Remove member option (confirmation modal) — Owner only

2. `RoleSelector.tsx`:
   - Dropdown/select with roles: Admin, Developer, Viewer
   - Owner role is NOT selectable (display only)
   - On change: API call to update role + toast confirmation
  </action>
</task>

<task type="auto">
  <name>Task 3: Invite member modal</name>
  <files>frontend/src/components/projects/InviteMemberModal.tsx</files>
  <action>
`InviteMemberModal.tsx`:
- Field: email address
- Field: role selector (default: Developer)
- "Invitar" button
- Error: "El usuario no existe en la plataforma" / "Ya es miembro del proyecto"
- Success: toast + member list refreshes
- Only visible to Owner/Admin
  </action>
</task>

<task type="auto">
  <name>Task 4: Project settings page</name>
  <files>frontend/src/pages/project/ProjectSettingsPage.tsx</files>
  <action>
`ProjectSettingsPage.tsx`:
- Section 1: "Información General" — edit name, description (uses ProjectForm)
- Section 2: "Equipo" — MemberList + InviteMemberModal trigger
- Section 3: "Zona de Peligro" (cardwith red border) — Archive project button (Owner only, confirmation modal)
- All sections in glass cards
- Permission-based: viewers see read-only versions
  </action>
</task>

</tasks>

<success_criteria>
- Project layout renders with tab navigation
- Settings page shows project info + team
- Invite modal adds members successfully
- Role changes work for Owner/Admin
- Archive project works with confirmation
- Phase 2 fully complete after this plan
</success_criteria>
