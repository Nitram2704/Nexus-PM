---
phase: 02-project-management
plan: 02
type: execute
wave: 5
depends_on: [02-01]
files_modified:
  - frontend/src/pages/dashboard/ProjectsDashboard.tsx
  - frontend/src/components/projects/ProjectCard.tsx
  - frontend/src/components/projects/CreateProjectModal.tsx
  - frontend/src/components/projects/ProjectForm.tsx
  - frontend/src/hooks/useProjects.ts
  - frontend/src/stores/projectStore.ts
autonomous: true

must_haves:
  truths:
    - "Dashboard shows grid of project cards with key info"
    - "Create project modal works with name + description"
    - "Empty state shown when no projects exist"
    - "Project cards show: name, key, member avatars, active sprint, last activity"
    - "Click on project navigates to project detail"
---

<objective>
Build the frontend projects dashboard: project cards grid, create/edit project modals, and project detail layout.

Purpose: The dashboard is the first thing a logged-in user sees. Must look premium with glass cards, stagger animations, and designed empty state.
Output: Logged-in user sees project dashboard, can create projects, navigate to project detail.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Projects dashboard page</name>
  <files>frontend/src/pages/dashboard/ProjectsDashboard.tsx, frontend/src/hooks/useProjects.ts</files>
  <action>
1. `useProjects.ts`: TanStack Query hook
   - `useProjects()`: fetches `projectsApi.list()`, returns { projects, isLoading }
   - `useCreateProject()`: mutation with cache invalidation
   - `useArchiveProject()`: mutation

2. `ProjectsDashboard.tsx`:
   - Header: "Mis Proyectos" + "Nuevo Proyecto" button (blue, with Plus icon)
   - Grid: 2 columns on md, 3 on lg
   - Each project renders `<ProjectCard />`
   - Loading: grid of `<Skeleton />` cards (3 items)
   - Empty state: `<EmptyState />` with folder icon, "Crea tu primer proyecto", CTA button
   - Stagger entrance animation on cards (Framer Motion)
   - Filter toggle: "Activos" / "Archivados"
  </action>
</task>

<task type="auto">
  <name>Task 2: Project card component</name>
  <files>frontend/src/components/projects/ProjectCard.tsx</files>
  <action>
`ProjectCard.tsx`:
- Glass card (`.card-glass`) with hover lift effect
- Content:
  - Top: Project key badge (JetBrains Mono, blue bg) + project name (Inter 600)
  - Description: 2-line truncated
  - Bottom row: avatar stack of members (max 4 + "+N"), active sprint badge ("Sprint 3" in green if active, gray if none), relative timestamp
- Click: navigates to `/project/{id}/backlog`
- Hover: border brightens, translateY(-2px)
  </action>
</task>

<task type="auto">
  <name>Task 3: Create project modal</name>
  <files>frontend/src/components/projects/CreateProjectModal.tsx, frontend/src/components/projects/ProjectForm.tsx</files>
  <action>
1. `CreateProjectModal.tsx`:
   - Trigger: "Nuevo Proyecto" button on dashboard
   - Uses `<Modal>` component
   - Contains `<ProjectForm />`

2. `ProjectForm.tsx`:
   - Fields: name (required, max 100), description (optional textarea, max 500)
   - Auto-preview of generated project key: as user types name, shows "Clave: NEX" below the input
   - "Crear Proyecto" button (loading state)
   - On success: close modal, toast "Proyecto creado", navigate to project
   - Validation: name required, min 2 characters
  </action>
</task>

</tasks>

<success_criteria>
- Dashboard renders project cards in responsive grid
- Empty state shows when no projects
- Create modal opens, validates, creates project, closes
- Project key preview updates as user types
- Cards have glass styling and stagger animation
- Navigation to project detail works
</success_criteria>
