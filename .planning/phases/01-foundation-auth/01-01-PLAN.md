---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/constants.ts
  - frontend/src/components/layout/Shell.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/layout/BottomNav.tsx
  - frontend/.env.local.example
  - frontend/.gitignore
autonomous: true

must_haves:
  truths:
    - "Frontend project starts with `npm run dev` on localhost:5173"
    - "Tailwind CSS dark mode renders correctly"
    - "Design tokens (colors, fonts) match DESIGN.md palette"
    - "Responsive layout renders: sidebar (desktop) + bottom nav (mobile)"
    - "Inter + JetBrains Mono fonts loaded from Google Fonts"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependencies and scripts"
      contains: "react, react-dom, react-router-dom, zustand, @tanstack/react-query, framer-motion, axios, clsx, tailwind-merge"
    - path: "frontend/src/index.css"
      provides: "Design tokens, glass utilities, dark theme"
    - path: "frontend/src/components/layout/Shell.tsx"
      provides: "Responsive layout shell"
      exports: ["Shell"]
    - path: "frontend/src/lib/utils.ts"
      provides: "Utility functions"
      exports: ["cn", "formatDate", "generateProjectKey"]
  key_links: []
---

<objective>
Scaffold the React frontend with Vite, Tailwind CSS, TypeScript, all dependencies, design tokens from DESIGN.md, and responsive layout shell.

Purpose: Establishes the frontend foundation — every other frontend work depends on this. After this plan, `npm run dev` works and the dark-mode responsive shell renders.
Output: A working React SPA with Vite, routing, dark theme, and responsive layout.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/DESIGN.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React + Vite project with all dependencies</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/.gitignore, frontend/.env.local.example</files>
  <action>
1. Create `frontend/` directory and initialize Vite React-TS project:
   ```bash
   npm create vite@latest frontend -- --template react-ts
   ```

2. Install production dependencies:
   - `react-router-dom@^6` — Client-side routing
   - `zustand@^4` — Global state management
   - `@tanstack/react-query@^5` — Server state / API caching
   - `axios@^1` — HTTP client with interceptors
   - `framer-motion@^12` — Animations
   - `react-beautiful-dnd@^13` — Drag & drop (Kanban)
   - `@types/react-beautiful-dnd` — DnD types
   - `lucide-react@^0.5` — Icons
   - `recharts@^2` — Charts (velocity, burndown)
   - `react-markdown@^9` — Markdown rendering
   - `react-hot-toast@^2` — Toast notifications
   - `clsx@^2` — Conditional classes
   - `tailwind-merge@^3` — Merge Tailwind classes
   - `date-fns@^4` — Date utilities

3. Install dev dependencies:
   - `tailwindcss@^4`, `@tailwindcss/postcss@^4`, `postcss`
   - All TypeScript types already included via Vite template

4. Create `.env.local.example`:
   ```
   VITE_API_URL=http://localhost:8000/api/v1
   ```

5. Configure `vite.config.ts` with proxy to Django backend:
   ```typescript
   server: {
     port: 5173,
     proxy: {
       '/api': {
         target: 'http://localhost:8000',
         changeOrigin: true,
       }
     }
   }
   ```
  </action>
  <verify>
- `cd frontend && npm run dev` starts on localhost:5173
- `.env.local.example` exists with API_URL
- All dependencies in package.json
  </verify>
  <done>React + Vite project scaffolded with all MVP dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create design tokens and global CSS</name>
  <files>frontend/src/index.css, frontend/index.html</files>
  <action>
1. `frontend/src/index.css`: Import Tailwind + define complete design system:
   - CSS custom properties from DESIGN.md color palette
   - Background layers: bg-1 through bg-4
   - Text colors: primary, secondary, muted
   - Priority colors: critical (red), high (orange), medium (blue), low (slate)
   - Glass card utilities: `.card-glass`, `.kanban-card`, `.ai-panel`
   - Grid background pattern (subtle tech aesthetic)
   - Scrollbar styling (dark theme)
   - Font loading: Inter (400-700) + JetBrains Mono (400-700)

2. `frontend/index.html`:
   - Google Fonts link for Inter + JetBrains Mono
   - `<html lang="es" class="dark">`
   - Meta tags: viewport, description, theme-color
   - Title: "Nexus-PM — Gestión Ágil con IA"

3. Tailwind config: extend with custom colors, fonts, and animations.
  </action>
  <verify>
- Dark mode renders with correct background colors
- Inter font applies to body text
- JetBrains Mono applies to `.font-mono` elements
- Grid background pattern visible (subtle)
  </verify>
  <done>Design tokens, glass utilities, and dark theme configured</done>
</task>

<task type="auto">
  <name>Task 3: Create utility functions</name>
  <files>frontend/src/lib/utils.ts, frontend/src/lib/constants.ts</files>
  <action>
1. `frontend/src/lib/utils.ts`:
   ```typescript
   import { clsx, type ClassValue } from 'clsx';
   import { twMerge } from 'tailwind-merge';
   import { format, formatDistanceToNow } from 'date-fns';
   import { es } from 'date-fns/locale';

   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }

   export function formatDate(date: string): string {
     return format(new Date(date), 'dd MMM yyyy', { locale: es });
   }

   export function formatRelative(date: string): string {
     return formatDistanceToNow(new Date(date), { addSuffix: true, locale: es });
   }

   export function generateProjectKey(name: string): string {
     return name.replace(/[^a-zA-Z]/g, '').substring(0, 3).toUpperCase();
   }
   ```

2. `frontend/src/lib/constants.ts`:
   ```typescript
   export const PRIORITY = {
     critical: { label: 'Crítica', color: '#EF4444', bg: 'bg-red-500/10' },
     high: { label: 'Alta', color: '#F97316', bg: 'bg-orange-500/10' },
     medium: { label: 'Media', color: '#3B82F6', bg: 'bg-blue-500/10' },
     low: { label: 'Baja', color: '#64748B', bg: 'bg-slate-500/10' },
   } as const;

   export const TASK_TYPE = {
     story: { label: 'Historia', icon: 'BookOpen', color: '#3B82F6' },
     bug: { label: 'Bug', icon: 'Bug', color: '#EF4444' },
     task: { label: 'Tarea', icon: 'CheckSquare', color: '#10B981' },
     epic: { label: 'Épica', icon: 'Layers', color: '#F59E0B' },
   } as const;

   export const SPRINT_STATUS = {
     planning: 'Planificación',
     active: 'Activo',
     completed: 'Completado',
   } as const;

   export const MEMBER_ROLE = {
     owner: 'Owner',
     admin: 'Admin',
     developer: 'Developer',
     viewer: 'Viewer',
   } as const;
   ```
  </action>
  <verify>
- `import { cn, formatDate } from '@/lib/utils'` compiles
- `import { PRIORITY, TASK_TYPE } from '@/lib/constants'` compiles
  </verify>
  <done>Utility functions and constants defined</done>
</task>

<task type="auto">
  <name>Task 4: Create responsive layout shell</name>
  <files>frontend/src/components/layout/Shell.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/components/layout/Header.tsx, frontend/src/components/layout/BottomNav.tsx, frontend/src/App.tsx, frontend/src/main.tsx</files>
  <action>
1. `Shell.tsx`: Responsive wrapper
   - Desktop (md+): Sidebar (left, fixed 260px) + Header (top) + Content (main area)
   - Mobile (<md): Header (top) + Content + BottomNav (fixed bottom)
   - Uses CSS `md:` breakpoints (not JS media queries)

2. `Sidebar.tsx`: Desktop navigation
   - Logo "Nexus-PM" with blue accent
   - Nav items: Dashboard, (dynamic: project submenu when inside a project)
   - Items: Backlog, Tablero, Reportes, Settings
   - Each item: Lucide icon + label + active state (blue left border + bg highlight)
   - Glass panel styling, fixed width 260px

3. `Header.tsx`: Top bar
   - Left: Page title (dynamic)
   - Right: Notification bell icon (with badge), User avatar + name dropdown
   - Glass panel styling with bottom border

4. `BottomNav.tsx`: Mobile bottom navigation
   - 4-5 items: Dashboard, Backlog, Board, Reports
   - Active state: blue icon + label
   - Glass panel styling, fixed bottom

5. `App.tsx`: React Router setup
   - Routes: `/login`, `/register`, `/forgot-password`
   - Routes: `/dashboard` (projects list)
   - Routes: `/project/:id/*` (project detail with sub-routes: backlog, board, reports, settings)
   - `Shell` wraps authenticated routes
   - Auth routes are standalone (no shell)

6. `main.tsx`: Entry point
   - `QueryClientProvider` (TanStack Query)
   - `BrowserRouter` (React Router)
   - `Toaster` (React Hot Toast)
   - Render `<App />`
  </action>
  <verify>
- `npm run dev` shows responsive layout
- Desktop: sidebar visible, bottom nav hidden
- Mobile (resize): sidebar hidden, bottom nav visible
- Navigation items render with icons
- Header renders with notification bell placeholder
  </verify>
  <done>Responsive layout shell renders with sidebar (desktop) and bottom nav (mobile)</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors on localhost:5173
2. Dark mode renders with correct palette (deep navy-black, not pure black)
3. Inter + JetBrains Mono fonts load from Google Fonts
4. Sidebar visible on desktop, bottom nav on mobile
5. All utility functions importable and working
6. React Router renders placeholder routes
7. No TypeScript errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- React + Vite app starts and renders dark-mode layout
- Design tokens from DESIGN.md applied correctly
- Responsive shell works: sidebar (desktop), bottom nav (mobile)
- All dependencies installed and importable
- Routing configured with placeholder pages
- No TypeScript or lint errors
</success_criteria>

<output>
After completion, update `.planning/STATE.md` to reflect Phase 1 Plan 1 complete.
</output>
