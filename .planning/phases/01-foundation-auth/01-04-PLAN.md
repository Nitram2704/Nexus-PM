---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - frontend/src/pages/settings/ProfilePage.tsx
  - frontend/src/components/ui/Avatar.tsx
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Input.tsx
  - frontend/src/components/ui/Modal.tsx
  - frontend/src/components/ui/Badge.tsx
  - frontend/src/components/ui/Skeleton.tsx
  - frontend/src/components/ui/EmptyState.tsx
  - backend/accounts/management/commands/seed_data.py
autonomous: true

must_haves:
  truths:
    - "User can edit profile (name, avatar) from settings page"
    - "Reusable UI components exist: Button, Input, Modal, Badge, Avatar, Skeleton, EmptyState"
    - "Seed data command creates demo users and project"
    - "All UI components follow glass/dark theme design tokens"
---

<objective>
Build the user profile page, create reusable UI component library (Button, Input, Modal, Badge, Avatar, Skeleton, EmptyState), and seed data management command.

Purpose: Establishes the shared UI component library used across ALL pages. Profile page completes the auth module. Seed data enables rapid testing.
Output: Profile page works, UI components available, `python manage.py seed_data` populates demo data.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reusable UI primitives</name>
  <files>frontend/src/components/ui/Button.tsx, frontend/src/components/ui/Input.tsx, frontend/src/components/ui/Modal.tsx, frontend/src/components/ui/Badge.tsx, frontend/src/components/ui/Avatar.tsx, frontend/src/components/ui/Skeleton.tsx, frontend/src/components/ui/EmptyState.tsx, frontend/src/components/ui/Card.tsx</files>
  <action>
Create reusable UI primitives following DESIGN.md tokens:

1. `Button.tsx`: Variants (primary/secondary/danger/ghost), sizes (sm/md/lg), loading state (spinner), disabled state. Blue accent primary. Scale-down 0.98 on tap (Framer Motion).

2. `Input.tsx`: Text/email/password types. Label, placeholder, error message display. Dark glass background input. Focus ring with blue accent. Password toggle eye icon.

3. `Modal.tsx`: Overlay (dark backdrop), glass card, close button, title, content slot. Framer Motion: fade in overlay + scale up card. Escape key closes. Click outside closes.

4. `Badge.tsx`: Variants matching priority colors (critical/high/medium/low). Also: type badges (story/bug/task/epic). Small rounded pill shape with colored background.

5. `Avatar.tsx`: Circular image with fallback (initials with colored background). Sizes: xs(24), sm(32), md(40), lg(56). Stack variant for showing multiple avatars overlapping.

6. `Skeleton.tsx`: Shimmer loading placeholder. Variants: text (single line), card (rectangle), circle (avatar). Animated gradient sweep.

7. `EmptyState.tsx`: Centered layout with icon (Lucide), title, description, optional CTA button. Used when lists are empty.

8. `Card.tsx`: Glass card wrapper with `.card-glass` styling. Hover lift effect. Optional header/footer slots.

All components:
- Use `cn()` for class merging
- Accept `className` prop for overrides
- TypeScript interfaces for all props
- Framer Motion where specified in DESIGN.md
  </action>
  <verify>
- All components render correctly in isolation
- Button shows loading spinner when `isLoading={true}`
- Modal opens/closes with animation
- Badge colors match DESIGN.md priority palette
- Skeleton has shimmer animation
  </verify>
</task>

<task type="auto">
  <name>Task 2: Profile page</name>
  <files>frontend/src/pages/settings/ProfilePage.tsx</files>
  <action>
1. Profile page at `/settings/profile`:
   - Header: "Mi Perfil"
   - Avatar upload: click avatar to change, preview before save
   - Form fields: first_name, last_name (editable), email (read-only, shown disabled)
   - Bio textarea
   - "Guardar Cambios" button
   - Toast on success: "Perfil actualizado"
   - Glass card layout matching auth page style
   - Uses UI components: Input, Button, Avatar, Card
  </action>
</task>

<task type="auto">
  <name>Task 3: Seed data management command</name>
  <files>backend/accounts/management/commands/seed_data.py</files>
  <action>
1. Django management command `seed_data`:
   ```bash
   python manage.py seed_data
   ```

2. Creates:
   - 3 users: admin@nexus.pm (owner), dev1@nexus.pm, dev2@nexus.pm (all password: "Test1234")
   - 1 project: "Nexus PM" (key: NEX), admin as owner, dev1+dev2 as developers
   - 4 default columns: Por Hacer, En Progreso, En Revisión, Hecho
   - 1 sprint: "Sprint 1" (active, 2 weeks)
   - 8 sample tasks across columns with different types/priorities
   - 3 labels: "frontend", "backend", "ux"

3. Idempotent: checks if data exists before creating
  </action>
  <verify>
- `python manage.py seed_data` runs without errors
- Login with admin@nexus.pm / Test1234 works
- Project "Nexus PM" visible in admin
- Sprint and tasks created
  </verify>
</task>

</tasks>

<success_criteria>
- UI component library complete with 8 primitives
- Profile page renders and saves changes
- Seed data creates realistic demo data
- All components follow dark-mode glass design tokens
- Phase 1 fully complete after this plan
</success_criteria>
