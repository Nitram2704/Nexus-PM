# 09-01: Fix Dependencies

**Phase:** 9 — Stabilization
**Tipo:** Backend / Config

## Context

`requirements.txt` está incompleto. Un fresh install rompe las features IA y opcionalmente PostgreSQL.

## Current requirements.txt
```
Django==5.1.4
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
python-decouple==3.8
django-filter>=24.0
# psycopg2-binary==2.9.9  (comentado)
```

## Faltantes verificados en el código
- `google-generativeai` — usado en `apps/intelligence/client.py`
- `Pillow` — comentado, esperar binarios de Python 3.14

## Proposed Changes

### [MODIFY] `backend/requirements.txt`
- Agregar `google-generativeai>=0.8.0`
- Mantener `psycopg2-binary` comentado con nota de cuándo instalarlo

## Task Breakdown
- [ ] T1: Agregar `google-generativeai` a requirements.txt
- [ ] T2: Crear venv limpio y verificar `pip install -r requirements.txt`
- [ ] T3: Verificar `python manage.py migrate && python manage.py runserver`
- [ ] T4: Verificar que las features IA funcionan (o caen a mock sin API key)

## Verification
```bash
cd backend
python -m venv .venv-test
.venv-test\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
Debe levantar sin ModuleNotFoundError.
