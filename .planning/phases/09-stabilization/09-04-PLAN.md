# 09-04: Environment Setup Docs

**Phase:** 9 — Stabilization
**Tipo:** Config / Docs

## Context

No hay `.env.example` para backend ni frontend. El README tiene instrucciones pero no lista
todas las variables. Un dev nuevo no sabe qué variables configurar.

## Proposed Changes

### [NEW] `backend/.env.example`
```env
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
GOOGLE_API_KEY=your-google-api-key-here
# PostgreSQL (opcional — omitir DB_HOST usa SQLite)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432
```

### [NEW] `frontend/.env.example`
```env
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_API_URL=http://localhost:8000/api
```

### [MODIFY] `README.md`
- Documentar todas las variables de entorno
- Aclarar que SQLite es el default (sin DB_HOST)
- Aclarar que GOOGLE_API_KEY es opcional (mock fallback)

## Task Breakdown
- [ ] T1: Crear `backend/.env.example`
- [ ] T2: Crear `frontend/.env.example`
- [ ] T3: Actualizar README con variables y notas de setup
- [ ] T4: Verificar que `.env` esté en `.gitignore` (ya está)

## Verification
- Ambos `.env.example` existen y listan todas las variables usadas en el código
- README refleja el setup real
