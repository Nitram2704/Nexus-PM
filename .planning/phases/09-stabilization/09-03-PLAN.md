# 09-03: Lint & Typecheck limpio

**Phase:** 9 — Stabilization
**Tipo:** Frontend + Backend

## Context

`npm run build` ejecuta `tsc -b && vite build`. `tsconfig.app.json` tiene `noUnusedLocals`
y `noUnusedParameters` activos — cualquier variable sin usar rompe el build. Hay que asegurar
que todo compile limpio.

## Task Breakdown
- [ ] T1: `npm run lint` — resolver todos los errores de ESLint
- [ ] T2: `npm run build` — resolver errores de TypeScript
- [ ] T3: `python manage.py check` — sin warnings
- [ ] T4: Verificar que no haya conflict markers (`<<<<<<<`) residuales en el código

## Verification
```bash
cd frontend
npm run lint      # 0 errores
npm run build     # compila sin errores
cd ../backend
python manage.py check   # sin warnings
```

## Notes
- El commit `7f9a0d8` menciona "resolve remaining conflict markers" — verificar que no queden.
- Si hay lints residuales en archivos de API (mencionado en PLAN-system-audit.md), resolverlos.
