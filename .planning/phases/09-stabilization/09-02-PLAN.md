# 09-02: Tests

**Phase:** 9 — Stabilization
**Tipo:** Backend + Frontend

## Context

Solo existe `backend/apps/projects/tests/test_analytics.py` y `projects/tests.py` (6KB).
No hay tests de frontend. No hay framework de testing de frontend configurado.

## Proposed Changes

### Backend (Django TestCase)
- [ ] `apps/accounts/tests.py` — registro, login, rate limiting, password reset
- [ ] `apps/projects/tests.py` — CRUD de proyectos, permisos por rol, invite
- [ ] `apps/tasks/tests.py` — CRUD de tareas, move, sprint start/complete, key generation
- [ ] `apps/intelligence/tests.py` — mock fallback (sin API key), import proposal

### Frontend (Vitest + React Testing Library)
- [ ] Instalar `vitest`, `@testing-library/react`, `jsdom`
- [ ] Configurar en `vite.config.ts` y `package.json` (`npm run test`)
- [ ] Tests básicos de stores (`authStore`, `projectStore`)
- [ ] Tests de `apiClient` (interceptor de token)

## Task Breakdown
- [ ] T1: Tests de accounts (auth flow)
- [ ] T2: Tests de projects (CRUD + permisos)
- [ ] T3: Tests de tasks (key generation, move, sprints)
- [ ] T4: Tests de intelligence (mock fallback)
- [ ] T5: Configurar Vitest en frontend
- [ ] T6: Tests básicos de stores y apiClient

## Verification
- `python manage.py test` — todos pasan
- `npm run test` — todos pasan
- Cobertura >60% en lógica core (auth, projects, tasks)

## Notes
- Para tests de intelligence, NO depender de la API real — usar el mock fallback (sin GOOGLE_API_KEY).
- Usar SQLite en memoria para tests de Django.
