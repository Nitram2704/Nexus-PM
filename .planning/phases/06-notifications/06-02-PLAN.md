---
phase: 06-notifications
plan: 02
type: execute
wave: 14
depends_on: [06-01, 04-02]
files_modified:
  - frontend/src/components/notifications/NotificationBell.tsx
  - frontend/src/components/notifications/NotificationDropdown.tsx
  - frontend/src/components/notifications/NotificationItem.tsx
  - frontend/src/pages/settings/NotificationSettingsPage.tsx
  - frontend/src/hooks/useNotifications.ts
  - frontend/src/components/layout/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Bell icon in header shows unread count badge"
    - "Clicking bell opens dropdown with notification list"
    - "Click notification navigates to relevant page and marks as read"
    - "Notification settings page with toggles per type"
    - "Polling every 30s for new notifications"
---

<objective>
Build frontend notifications: bell icon with badge, dropdown list, navigation on click, settings page, polling for new notifications.

Purpose: Keeps users informed and engaged. The bell icon is always visible in the header.
Output: Working notification system with bell, dropdown, navigation, and settings.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Notification hooks</name>
  <files>frontend/src/hooks/useNotifications.ts</files>
  <action>
- `useUnreadCount()`: polls every 30s for unread count (TanStack Query with refetchInterval)
- `useNotifications()`: fetches notification list
- `useMarkRead()`: mutation to mark notification(s) as read
- `useNotificationPreferences()`: get/update preferences
  </action>
</task>

<task type="auto">
  <name>Task 2: Bell and dropdown</name>
  <files>frontend/src/components/notifications/NotificationBell.tsx, frontend/src/components/notifications/NotificationDropdown.tsx, frontend/src/components/notifications/NotificationItem.tsx</files>
  <action>
1. `NotificationBell.tsx`:
   - Bell icon (Lucide: Bell)
   - Red badge with unread count (hidden if 0)
   - Badge animates (scale bounce) when count increases
   - Click toggles dropdown

2. `NotificationDropdown.tsx`:
   - Glass panel dropdown below bell
   - Header: "Notificaciones" + "Marcar todas como leídas" link
   - List of `<NotificationItem />`
   - Empty: "No tienes notificaciones"
   - Max 10 items, "Ver todas" link for full list
   - Framer Motion: slide down + fade in

3. `NotificationItem.tsx`:
   - Icon by type (Bell for task, Clock for sprint, Bot for AI)
   - Title + message (truncated)
   - Relative time (JetBrains Mono)
   - Unread indicator (blue dot on left)
   - Click: navigate to link + mark read
   - Hover: bg highlight
  </action>
</task>

<task type="auto">
  <name>Task 3: Notification settings</name>
  <files>frontend/src/pages/settings/NotificationSettingsPage.tsx</files>
  <action>
- Section in user settings
- Toggle cards for each type:
  - Asignación de tareas (default: on)
  - Deadline de sprint (default: on)
  - Recomendaciones IA (default: on)
  - Menciones (default: on)
- Each toggle: glass card with icon, label, description, switch
- Changes save immediately (auto-save with debounce)
- Toast: "Preferencias actualizadas"
  </action>
</task>

<task type="auto">
  <name>Task 4: Integrate bell into header</name>
  <files>frontend/src/components/layout/Header.tsx</files>
  <action>
Update Header component:
- Add `<NotificationBell />` before user avatar
- Ensure dropdown doesn't overflow viewport
- Close dropdown when clicking outside
  </action>
</task>

</tasks>

<success_criteria>
- Bell shows correct unread count
- Dropdown shows notifications with icons and timestamps
- Click navigates to relevant page
- Settings toggles save preferences
- Polling updates count every 30s
- Phase 6 complete after this plan
</success_criteria>
