---
phase: 04-kanban-board
plan: 03
type: execute
wave: 10
depends_on: [04-02]
files_modified:
  - frontend/src/index.css
  - frontend/src/components/ui/Tooltip.tsx
  - frontend/src/components/ui/Dropdown.tsx
  - frontend/src/components/ui/Tabs.tsx
  - frontend/src/components/ui/ProgressBar.tsx
  - frontend/src/components/layout/Shell.tsx
  - frontend/src/components/layout/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "All UI surfaces use glass card styling with correct depth layers"
    - "Micro-animations on all interactive elements (hover, tap, transition)"
    - "Responsive: mobile shows simplified board with horizontal scroll"
    - "No raw Tailwind gray backgrounds — all use custom HSL palette"
    - "Additional UI components: Tooltip, Dropdown, Tabs, ProgressBar"
---

<objective>
Polish the UI: ensure all surfaces use glass styling, add missing UI components (Tooltip, Dropdown, Tabs, ProgressBar), refine responsive behavior, and implement all micro-animations from DESIGN.md.

Purpose: Premium look & feel. This plan transforms functional UI into polished, professional UI.
Output: Every surface looks premium, all animations smooth, responsive works correctly.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Additional UI components</name>
  <action>
1. `Tooltip.tsx`: Hover tooltip using Framer Motion. Dark glass bg, arrow, fade/scale animation. Configurable position (top/right/bottom/left).

2. `Dropdown.tsx`: Dropdown menu with glass styling. Opens on click/hover. Items with icons. Keyboard navigation (arrow keys, escape). Framer Motion entrance.

3. `Tabs.tsx`: Tab bar with animated underline indicator that slides to active tab. Glass panel for content area. Smooth content transition.

4. `ProgressBar.tsx`: Animated progress bar. Color shifts based on progress (green: on track, orange: behind, red: critical). Shimmer animation on fill. Configurable size (sm/md/lg). Label option: "24/34 pts".
  </action>
</task>

<task type="auto">
  <name>Task 2: Global animation audit</name>
  <action>
Review ALL existing components and ensure:
- Page transitions: use `pageTransition` variant from DESIGN.md
- Lists: use stagger pattern (0.06s between children)
- Cards: hover lift (translateY -2px)
- Buttons: tap scale (0.98)
- Modals: fade overlay + scale card
- Inputs: focus ring animation (blue glow)
- Toasts: slide in from top-right
- Loading: skeleton shimmer (NOT spinners)
- Tab indicators: sliding underline
- Badge changes: brief scale-bounce

Add Framer Motion `<AnimatePresence>` where content changes dynamically.
  </action>
</task>

<task type="auto">
  <name>Task 3: Responsive refinement</name>
  <action>
1. Desktop (1280px+): Full sidebar (260px) + content
2. Tablet (768px-1279px): Collapsible sidebar (icon-only mode, 64px) + content
3. Mobile (<768px): No sidebar + bottom nav + hamburger menu for full nav

Kanban board on mobile:
- Columns stack horizontally with snap scroll
- Cards are full-width within column
- Simplified card (no labels/points, just key + title + priority border)

Backlog on mobile:
- Simplified list items (no drag handle — reorder via menu)
- Filter bar collapses to filter button with badge count

Ensure touch targets are 44px+ on mobile.
  </action>
</task>

<task type="auto">
  <name>Task 4: CSS polish</name>
  <action>
1. Audit `index.css`: ensure all custom properties are used, no hardcoded colors
2. Add CSS grid background pattern (subtle dots or lines)
3. Custom scrollbar styling for dark mode
4. Focus-visible styles (keyboard navigation accessibility)
5. Selection color (::selection with blue accent)
6. Smooth scroll behavior
7. Print media query (hide sidebar, nav)
  </action>
</task>

</tasks>

<success_criteria>
- Every surface uses glass card styling, NOT flat gray
- All interactions have micro-animations
- Responsive works on desktop, tablet, mobile
- No raw Tailwind backgrounds — all custom palette
- UI components complete: Tooltip, Dropdown, Tabs, ProgressBar
- Phase 4 complete after this plan
</success_criteria>
