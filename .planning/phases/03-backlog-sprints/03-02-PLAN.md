---
phase: 03-backlog-sprints
plan: 02
type: execute
wave: 7
depends_on: [03-01]
files_modified:
  - frontend/src/pages/project/BacklogPage.tsx
  - frontend/src/components/backlog/BacklogList.tsx
  - frontend/src/components/backlog/BacklogItem.tsx
  - frontend/src/components/backlog/BacklogFilters.tsx
  - frontend/src/components/backlog/CreateTaskModal.tsx
  - frontend/src/components/backlog/TaskForm.tsx
  - frontend/src/hooks/useBacklog.ts
autonomous: true

must_haves:
  truths:
    - "Backlog page shows sortable list of tasks with drag handles"
    - "Each item shows: key (mono), title, type icon, priority badge, assignee avatar, story points"
    - "Filter bar with type, priority, assignee, text search"
    - "Create task modal with full form (title, description with Markdown preview, type, priority, story points, labels, assignee)"
    - "Drag to reorder persists via API"
---

<objective>
Build the frontend backlog view: sortable task list, filter bar, create/edit task modal, and drag-to-reorder.

Purpose: The backlog is where teams manage all work items. Must support filtering, ordering, and quick creation.
Output: Interactive backlog list with drag reorder, filters, and create modal.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Backlog page and hooks</name>
  <files>frontend/src/pages/project/BacklogPage.tsx, frontend/src/hooks/useBacklog.ts</files>
  <action>
1. `useBacklog.ts`: TanStack Query hooks
   - `useBacklogItems(projectId, filters)`: fetch tasks with filters
   - `useCreateTask(projectId)`: mutation
   - `useUpdateTask(projectId)`: mutation with optimistic update
   - `useReorderBacklog(projectId)`: mutation for drag reorder

2. `BacklogPage.tsx`:
   - Header: "Backlog" + task count badge + "Nuevo Ítem" button + "Generar con IA" button (placeholder for Phase 5)
   - Filter bar: `<BacklogFilters />`
   - Task list: `<BacklogList />`
   - Empty state if no tasks
   - Loading: skeleton list
  </action>
</task>

<task type="auto">
  <name>Task 2: Backlog list with drag reorder</name>
  <files>frontend/src/components/backlog/BacklogList.tsx, frontend/src/components/backlog/BacklogItem.tsx</files>
  <action>
1. `BacklogList.tsx`:
   - Uses React Beautiful DnD `<DragDropContext>` + `<Droppable>`
   - Renders a list of `<BacklogItem />` wrapped in `<Draggable>`
   - On drag end: calculates new order values, calls reorder API
   - Stagger entrance animation

2. `BacklogItem.tsx`:
   - Drag handle (grip icon, left side)
   - Task key: JetBrains Mono, blue text (e.g., "NEX-015")
   - Title: Inter 500
   - Type: small icon (BookOpen for story, Bug for bug, etc.)
   - Priority: colored badge (Crítica=red, Alta=orange, Media=blue, Baja=gray)
   - Assignee: small avatar (or unassigned icon)
   - Story points: badge with number (JetBrains Mono)
   - Labels: tiny colored dots
   - Click: opens task detail modal
   - Hover: slight bg highlight
  </action>
</task>

<task type="auto">
  <name>Task 3: Filter bar</name>
  <files>frontend/src/components/backlog/BacklogFilters.tsx</files>
  <action>
- Horizontal bar with filter controls:
  - Type: multi-select dropdown (Historia, Bug, Tarea, Épica)
  - Priority: multi-select dropdown
  - Assignee: avatar selector dropdown
  - Text search: input with search icon (debounced 300ms)
- "Limpiar filtros" button when filters are active
- Result count: "12 ítems" badge
- Filters stored in URL search params (shareable)
  </action>
</task>

<task type="auto">
  <name>Task 4: Create/Edit task modal</name>
  <files>frontend/src/components/backlog/CreateTaskModal.tsx, frontend/src/components/backlog/TaskForm.tsx</files>
  <action>
1. `TaskForm.tsx`:
   - Title: text input (required)
   - Description: textarea with Markdown preview toggle (split view or tabs)
   - Type: button group (Historia/Bug/Tarea/Épica) with icons
   - Priority: button group (Crítica/Alta/Media/Baja) with colors
   - Story Points: Fibonacci buttons (1,2,3,5,8,13,21)
   - Assignee: dropdown with team member avatars
   - Labels: multi-select or tag input
   - Sprint: dropdown (optional, for assigning to sprint)

2. `CreateTaskModal.tsx`:
   - Uses `<Modal>` + `<TaskForm />`
   - Create mode: empty form, "Crear Ítem" button
   - Edit mode: pre-filled form, "Guardar Cambios" button
   - On success: close modal, toast, refresh backlog list
  </action>
</task>

</tasks>

<success_criteria>
- Backlog list renders with all task data fields
- Drag to reorder works and persists
- Filters combine correctly (AND logic)
- Create task modal validates and creates items
- Task key auto-generates in JetBrains Mono style
- Stagger animation on list entrance
</success_criteria>
