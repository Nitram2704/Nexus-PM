---
phase: 03-backlog-sprints
plan: 04
type: execute
wave: 8
depends_on: [03-02, 03-03]
files_modified:
  - frontend/src/components/backlog/SprintPlanning.tsx
  - frontend/src/components/backlog/SprintCard.tsx
  - frontend/src/components/backlog/CompleteSprintModal.tsx
  - frontend/src/components/backlog/TaskDetailPanel.tsx
  - frontend/src/components/backlog/CommentThread.tsx
  - frontend/src/hooks/useSprints.ts
autonomous: true

must_haves:
  truths:
    - "Sprint planning view shows sprint info + drag tasks from backlog to sprint"
    - "Sprint card shows: name, dates, story points total, progress bar"
    - "Complete sprint modal shows completed vs incomplete breakdown"
    - "Task detail panel shows full info, comments thread, and edit capability"
---

<objective>
Build frontend sprint planning: sprint card, drag-to-assign from backlog, sprint completion modal, task detail panel with comments.

Purpose: Sprint planning is the core planning ceremony view. Task detail panel is reused in Kanban (Phase 4).
Output: Complete sprint planning with drag assignment and task detail view.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Sprint planning layout</name>
  <files>frontend/src/components/backlog/SprintPlanning.tsx, frontend/src/components/backlog/SprintCard.tsx, frontend/src/hooks/useSprints.ts</files>
  <action>
1. `useSprints.ts`: TanStack Query hooks for sprint operations

2. `SprintPlanning.tsx`:
   - Split view: backlog (left 60%) | sprint (right 40%)
   - Sprint section shows `<SprintCard />` + task list assigned to sprint
   - Drag tasks from backlog list → sprint drop zone
   - "Crear Sprint" button if no sprint in planning state
   - "Iniciar Sprint" button on planning sprint (Owner/Admin only)

3. `SprintCard.tsx`:
   - Sprint name + status badge
   - Date range: "14 abr – 28 abr" (JetBrains Mono)
   - Goal text (if set)
   - Story points: "24/34 pts" with progress bar
   - Task count: "12 tareas"
   - Days remaining badge (countdown, if active)
  </action>
</task>

<task type="auto">
  <name>Task 2: Complete sprint modal</name>
  <files>frontend/src/components/backlog/CompleteSprintModal.tsx</files>
  <action>
- Summary: completed tasks (with green checks) + incomplete tasks (with orange warnings)
- Story points burned vs planned
- Radio buttons for incomplete tasks: "Mover al backlog" / "Mover al siguiente sprint"
- "Completar Sprint" button
- Optional: "Generar resumen con IA" checkbox (placeholder for Phase 5)
  </action>
</task>

<task type="auto">
  <name>Task 3: Task detail panel</name>
  <files>frontend/src/components/backlog/TaskDetailPanel.tsx, frontend/src/components/backlog/CommentThread.tsx</files>
  <action>
1. `TaskDetailPanel.tsx`:
   - Slide-in panel from right (or modal)
   - Header: task key (NEX-015) + title (editable inline)
   - Sidebar fields: type, priority, assignee, story points, labels, sprint (all editable)
   - Description: Markdown editor with preview
   - Tabs: "Comentarios" | "Historial"
   - Comments tab: `<CommentThread />`
   - History tab: list of changes (who changed what, when)
   - Close button returns to previous view

2. `CommentThread.tsx`:
   - List of comments: avatar + name + timestamp + content
   - New comment input at bottom with "Comentar" button
   - Markdown support in comments
   - Stagger animation for existing comments
  </action>
</task>

</tasks>

<success_criteria>
- Sprint planning drag-to-assign works
- Sprint card shows live metric calculations
- Complete sprint modal handles incomplete tasks
- Task detail panel shows all fields and supports editing
- Comment thread works with real-time additions
- Phase 3 fully complete after this plan
</success_criteria>
